(version 1)

(rule "USB_Outer_Layer"
   # from saturn pcb calculator - with ground plane one below.
  (layer outer)
  (constraint hole_to_hole (min 1.2mm))
  (constraint via_diameter (min 1mm) (opt 1mm) (max 1.5mm))
  (constraint hole_size (opt 0.3mm))
  (condition "A.NetClass == 'USB'")
)

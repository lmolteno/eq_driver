# OpenOCD board configuration for RP2040 telescope mount controller
# Uses CMSIS-DAP interface (Pico Debug Probe or similar)

source [find interface/cmsis-dap.cfg]
source [find target/rp2040.cfg]

# Adapter speed
adapter speed 5000

